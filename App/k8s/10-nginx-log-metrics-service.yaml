apiVersion: v1
kind: Service
metadata:
  name: nginx-log-metrics
  namespace: php-nginx-demo
  labels:
    app: nginx
    metrics: log
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - name: log-metrics
      port: 4040
      targetPort: log-metrics
